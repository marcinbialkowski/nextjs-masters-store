extend type Query {
  product(slug: String!): Product
  products(first: Int, skip: Int): ProductList!
}

type Product {
  id: ID!
  slug: String!
  name: String!
  description: String!
  price: Int!
  images: [Image!]!
}

type ProductList {
  data: [Product!]!
  meta: ListMeta!
}
