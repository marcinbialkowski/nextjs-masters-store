extend type Query {
  product(slug: String!): Product
  products(first: Int, skip: Int): ProductList!
}

type Product {
  id: ID!
  name: String!
  slug: String!
  description: String!
  price: Int!
  images: [Image!]!
}

type ProductList {
  data: [Product!]!
  meta: ListMeta!
}
