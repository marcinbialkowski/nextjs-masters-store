mutation ReviewCreate(
  $productId: ID!
  $author: String!
  $email: String!
  $title: String!
  $content: String!
  $rating: Int!
) {
  reviewCreate(
    productId: $productId
    author: $author
    email: $email
    title: $title
    content: $content
    rating: $rating
  ) {
    id
  }
}
